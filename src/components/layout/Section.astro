---
const { bgColor = "", innerClass = "", id = "" } = Astro.props;
---

<section id={id} class={`w-full py-16 ${bgColor}`}>
  <div class={`max-w-[1440px] mx-auto px-5 lg:px-20 flex flex-col gap-12 ${innerClass}`}>
    <slot />
  </div>
</section>
