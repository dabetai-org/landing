---
const { bgColor = "", innerClass = "", id = "" } = Astro.props;
---

<section id={id} class={`w-full py-10 sm:py-16 lg:py-[120px] ${bgColor}`}>
  <div class={`max-w-[1440px] mx-auto px-6 sm:px-10 lg:px-20 flex flex-col gap-8 sm:gap-10 lg:gap-12 ${innerClass}`}>
    <slot />
  </div>
</section>
